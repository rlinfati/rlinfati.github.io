# rlinfati.github.io

themes -> https://github.com/calintat/minimal/

git reset --hard

git submodule status

git submodule update --init

git submodule foreach git pull origin master

git submodule status

git commit -a

git update-ref -d HEAD

